# Version control
.git
.gitignore

# Dev / test artefacts
node_modules
npm-debug.log*
test

# Local mail data – mounted as a volume at runtime
data

# Docker files (not needed inside the image)
Dockerfile
docker-compose.yml
.dockerignore

# Documentation
README.md
